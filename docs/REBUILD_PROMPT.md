# REBUILD_PROMPT.md — one-shot rebuild prompt (creditmax)

> Paste this entire block into a fresh AI session to rebuild Panther Digital from scratch. Model: **muse-spark-1.2-contributor-free** (opencode-free) — keep everything free/local.

```
Rebuild Panther Digital (Emergent Matrix) from scratch at C:/emergent-matrix — Next.js 16.3.3 Turbopack + React 19 + Tailwind 4 + three 0.185 + zustand 5 + Privy 3.38 (env-gated, graceful fallback, never hang). Workdir C:/emergent-matrix.

SPECS:
- Launch /: black-marble panther hero (/black-marble-panther.jpg veil 0.94, launch-veil/vignette), 1100ms loader (panther-icon ring+bar, skip if prefers-reduced-motion), PANTHER DIGITAL / EMERGENT MATRIX / huge LAUNCH APP → /app only, footer NOT FINANCIAL ADVICE.
- App /app (client, ~920 lines): CoinGecko live feed 300 coins = 3 pages ×100 via server proxy GET /api/coins/markets?page=&per_page= (x-cg-demo-api-key header from COINGECKO_API_KEY/HELIUS_API_KEY never hardcode, 429 retry 900*2^attempt 3 retries respecting Retry-After, s-maxage 30 stale 60 CORS *, keep last-good feed, 120s refresh). Calc emergentScore 0-100 + risk/trend (Breaking/Heating/Volatile/Stealth/Cooling) + spark; ScoreRing SVG r17 dash 2πr*score/100  — 90+ = orange #FF6B00 glow drop-shadow, dashed outer r+4, ping border, star. Ticker: doubled coins, 600s scroll, ticker-marble overlay, gainers emerald+shadow, surging (change24h>=8||trend Breaking) = !border orange surge-glow shadow ring + 🔥 SURGING pill + pulsing icon. Chain pills (All/Solana/Ethereum/Base/Robinhood/Sui) + Bucket pills (Layer1/DeFi/Meme/AI/Gaming/Stable/RWA/Infra) with /assets/mapped marble icons; Robinhood active = orange glow scale 1.06 ring. Sort default emergentScore (trophy), search, watchlist, showMore. Panels: Radar (real/Emerging/Breaking counts), Market Pulse (top3 volume, meme index, BTC dom via /api/global), Fear&Greedy /api/fng bar, News /api/news, Dex boosts /api/dex (8 boosts 60s), Panther AI trader (Breaking/Heating top5 sorted score, entry ±3% simulated PnL), AI Spotlight (AI bucket top4), X-scan KOL /api/x-scan (90s), RWA slice, NFTs waterfall (opensea→cg nfts/list+detail→blueIds bayc/punks/azuki…→dex boosts, 24h/7d/30d scaled), detail drawer (chart, holders top10%, dex pool, win%/vol). Header sticky logo Link href="/" always (panther-icon size-9 border) + minimal 3 icons + Connect; Connect modal (black/85 marble, 440px rounded 28) offers Privy/Wallet+ X + MetaMask+Phantom+Coinbase (Base deep-link fallback, never Privy warning banner). Build must be ✓ 1239-1240 pages SSG (TCG cards 1109+ sets 42 talents 67) with one expected warning @pixiv/three-vrm optional.

- Waifus /waifus: Boss Lucy (@sageglowsbot, lucy-work.png SFW) delegates to 4 via Hermes crons: Rias 🌸 head/panther-nav-ux every 360m 00a627273861 (owns nav/logo→/, marble ticker, /waifus glass), Kuro 🐆 panther-market every 30m 1d4a12cf83bd (src/app/api/coins/markets+ calc), Hikari ⚡ panther-ticker-feed every 120m f046435c05a7 (ticker+surging+90 glow), Mio 🔐 panther-auth every 360m 79d57a7e9bb9 (PrivyProvider). Style: Meta AI share marble jungle glass — /home-bg.jpg + veils + glass bg-white/75 backdrop-blur-xl rounded 2xl/3xl, single column max 720, header glass + gateway alive pulse. Only lucy-work.png + rias-waifu.png rendered here (lucy-private.png exists but hidden). Tick now 1s. Configure via `hermes cron add --name $ --schedule "every Xm" --workdir C:/emergent-matrix`.

- Avatar /avatar + Holo: src/components/HoloProjector.tsx CSS-only floating Lucy (float 3.2s, glow radial, scan 7px, cyan/magenta fringe ±0.9px screen, beam polygon, disk 112x34, flicker 120ms/3-5s, respects prefers-reduced-motion, fallback rias-waifu) + public/holo.html preview; /avatar page with LucyAvatar.tsx (three GLTFLoader+VRMLoaderPlugin lazy, lights ambient+dir+fill, breath sway, blink, auto-rotate, fallback 2D crossfade 4200ms among 3 avis) and public/avatar.vrm placeholder 1KB + avatar.config.json (placeholder true, tracking kalidokit+mediapipe disabled). Keep SFW, credit-light. Build warning three-vrm expected.

- ComfyUI C:/ComfyUI with Animagine XL 3.1 (6.5G models/checkpoints/animagine-xl-3.1.safetensors) + fallback Juggernaut/DreamShaper; nodes IPAdapter_plus + controlnet_aux (DW Pose) → workflow docs/comfy-lucy-workflow.json (12 nodes: checkpoint → LoadImage×2 rias+lucy → IPAdapter 0.70/0.62 + ControlNet openpose T-pose → CLIP positive t-pose character sheet…, negative 3d blurry…, KSampler 28 CFG6.5 euler_a) → output/lucy/*.png → public/avatar/texture/ → VRoid Studio free export VRM 1.0 → /avatar.vrm. Docs: docs/avatar.md 292 lines.

- Ollama moondream vision (free local, RTX4050): ollama pull moondream; not required for build, creditmax fallback is no-op if missing.

- Avis: public/lucy-work.png (Lucy boss), rias-waifu.png (Rias canonical), lucy-private.png (private ref), black-marble-panther.jpg/home-bg.jpg, panther-icon.png, panther.glb 65M + meshy jpgs, avatar.vrm placeholder, holo.html.

- Hobby navi C:/waifu-navi (separate Next 16 app): 2×2 roster Rias/Lucy/Astra/Lux + HoloProjector + ChatPanel + /api/crons (8s poll) + canon docs — scaffold in progress, shares avis via copy.

- Env only keys (never hardcode): COINGECKO_API_KEY, HELIUS_API_KEY, NEXT_PUBLIC_COINGECKO_API_KEY mirrored, OPENSEA optional, NEXT_PUBLIC_PRIVY_APP_ID optional (fallback no banner), NEXT_PUBLIC_ETHERSCAN optional, Vercel OIDC in .env.vercel.tmp. DexScreener keyless.

- Commands: npm install; npm run dev (:3000); npm run build (verify 1240 pages, 3.2s); npx tsc --noEmit; hermes cron list; ollama pull moondream; git log shows e36a802 Market pulse… +5. Patches: ticker gainers/surging glow, logo Link /, no Privy banner, CG 429 backoff (server + client), Robinhood orange ping, score sort. Voice Jessica ElevenLabs→Jenny fallback (or edge), Telegram @sageglowsbot.

Keep 100% free — no paid APIs, creditmax muse-spark. Document to docs/BUILD.md with overview/stack/waifus+crons/ComfyUI+Ollama/avis/patch-notes+1240 build/file map, and this REBUILD_PROMPT.md.
```
