# texture/

Drop your ComfyUI outputs here so VRoid can consume them.

```
public/avatar/texture/
├── alina_tpose_front.png   ← front T-pose (white bg) from ComfyUI §2.2
├── alina_tpose_side.png    ← side
├── alina_tpose_back.png    ← back
└── alina_face_closeup.png  ← face detail
```

Generate with `rias-waifu.png` + `lucy-work.png` as IPAdapter refs (see docs/avatar.md §2.2).
These are local references only — never commit NSFW. Keep SFW, credit-light.
